name: merxen
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - spatialdata @ git+https://github.com/scverse/spatialdata.git@v0.7.3a1
      - -e ".[dev]"
      # GPU acceleration (optional): pip install -e ".[gpu]" --extra-index-url=https://pypi.nvidia.com
      # - rapids-singlecell-cu12[rapids]
